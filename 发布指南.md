# 📤 发布指南

## GitHub 发布步骤

由于 GitHub App token 权限限制，请按以下步骤手动完成发布：

### 方法一：使用 Git 命令行

```bash
# 1. 进入项目目录
cd "C:\Users\lieying\WorkBuddy\2026-05-07-task-2"

# 2. 初始化 Git（如果尚未初始化）
git init

# 3. 添加远程仓库
git remote add origin https://github.com/lieying5198/headhunting-power-station.git

# 4. 添加所有文件
git add .

# 5. 提交
git commit -m "feat: 猎头AI加油站 Skill v1.0.0 - 会员管理与分销系统"

# 6. 推送到 GitHub
git push -u origin main
```

### 方法二：创建新仓库后推送

```bash
# 1. 在 GitHub 网页上创建新仓库
#    访问: https://github.com/new
#    仓库名称: headhunting-power-station
#    描述: 猎头全方位能力提升与精准交付赋能系统
#    选择: Public
#    不勾选: Add a README file（因为我们已有）

# 2. 进入项目目录
cd "C:\Users\lieying\WorkBuddy\2026-05-07-task-2"

# 3. 初始化 Git
git init

# 4. 添加远程仓库
git remote add origin https://github.com/lieying5198/headhunting-power-station.git

# 5. 添加所有文件
git add .

# 6. 提交
git commit -m "feat: 猎头AI加油站 Skill v1.0.0 - 会员管理与分销系统"

# 7. 推送
git push -u origin main
```

### 方法三：推送到现有仓库 (lieyingmeng)

如果您想将 Skill 代码添加到已有的 `lieyingmeng` 仓库：

```bash
# 1. 进入项目目录
cd "C:\Users\lieying\WorkBuddy\2026-05-07-task-2"

# 2. 初始化 Git
git init

# 3. 添加远程仓库
git remote add origin https://github.com/lieying5198/lieyingmeng.git

# 4. 创建并切换到新分支
git checkout -b feature/headhunting-power-station

# 5. 添加所有文件
git add .

# 6. 提交
git commit -m "feat: 添加猎头AI加油站 Skill v1.0.0"

# 7. 推送
git push -u origin feature/headhunting-power-station

# 8. 在 GitHub 上创建 Pull Request
```

## 仓库结构

```
headhunting-power-station/
├── handlers/              # 核心业务模块
│   ├── membership.js       # 会员管理
│   ├── reward.js          # 分销奖励
│   ├── admin.js           # 管理员功能
│   ├── database.js         # 数据存储
│   ├── notification.js     # 通知系统
│   └── commands.js         # 命令路由
├── data/                   # JSON数据文件
├── scripts/                # 工具脚本
│   ├── init-data.js       # 初始化数据
│   └── settlement.js       # 月度结算
├── admin-cli.js           # 管理员CLI
├── demo.js                # 演示脚本
├── index.js               # 主入口
├── test.js                # 测试脚本
├── manifest.json          # Skill配置
├── package.json           # 项目配置
├── README.md              # 说明文档
├── system_prompt.md       # 系统提示词
├── skill.json             # Skill定义
└── 发布指南.md            # 本文件
```

## 核心功能

### ✅ 已实现功能

1. **会员管理**
   - 用户注册（支持推荐人）
   - 会员升级（月卡/年卡/终身）
   - 每日使用限额控制
   - 用户状态查询

2. **分销系统**
   - 二级分销机制（20% + 5%）
   - 推荐码生成
   - 佣金自动计算
   - 收益明细查询

3. **提现系统**
   - 最低提现金额 ¥100
   - 提现申请审批流程
   - 冻结金额管理

4. **管理员功能**
   - 会员列表管理
   - 收益统计报表
   - 提现审批管理
   - 排行榜查看

5. **政策配置**
   - ✅ 微信支付（唯一支付方式）
   - ✅ 一经注册，不予退费

## 测试验证

发布前建议运行测试验证功能：

```bash
cd "C:\Users\lieying\WorkBuddy\2026-05-07-task-2"
npm install
npm test
```

## GitHub Token 权限说明

当前 GitHub MCP 工具的 token 可能缺少 `repo` 权限，因此无法通过 API 直接创建仓库或推送代码。

**解决方案**：
1. 使用上述 Git 命令行方式发布
2. 或在 GitHub 设置中检查 GitHub App 的权限配置
3. 或使用 Personal Access Token (PAT) 替代

---
*生成时间: 2026-05-07*
